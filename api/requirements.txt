# Production serverless runtime dependencies for Vercel
# Heavy ML training packages (scikit-learn, scipy, pandas) and testing frameworks (pytest, mongomock)
# are kept in backend/requirements.txt for local development and offline model training.
Flask>=3.0.0
flask-cors>=4.0.0
flask-limiter>=3.5.0
pymongo>=4.6.0
PyJWT>=2.8.0
Werkzeug>=3.0.0
python-dotenv>=1.0.0
requests>=2.31.0
